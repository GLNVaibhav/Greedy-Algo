HealthQueue — simple backend + corrective frontend

This repo was copied from the user's workspace and contains a backend Express API and a Vite+React frontend.

To run backend:

```bash
cd backend
npm install
npm start
```

To run frontend dev (optional):

```bash
cd frontend
npm install
npm run dev
```
